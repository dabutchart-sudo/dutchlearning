export function dutchVoice(){const voices=globalThis.speechSynthesis?.getVoices().filter(v=>/^nl(?:-|_)/i.test(v.lang))||[];return voices.find(v=>v.localService)||voices[0];}
export function speak(text,onError=()=>{}){const voice=dutchVoice();if(!voice){onError('No Dutch voice is available. Install a Dutch voice in your device settings, or keep practising with text.');return false;}speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.voice=voice;u.lang='nl-NL';u.rate=.85;u.onerror=()=>onError('Audio could not play. You can switch this listening question to text.');speechSynthesis.speak(u);return true;}
